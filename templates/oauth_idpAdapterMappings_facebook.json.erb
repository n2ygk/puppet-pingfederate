{
  "id": "Facebook",<%# hardcoded ID %>
  "idpAdapterRef": {
    "id": "Facebook", 
    "location": "<%= @adm_api_baseURL -%>/idp/adapters/Facebook"
  },
  "attributeContractFulfillment": {
<% @facebook_oauth_idp_map.each_with_index do |val, i| -%>
      "<%= val['name'] -%>": {
        "source": {
          "type": "<%= val['type'] -%>"
        },
        "value": "<%= val['value'] -%>"
      }<%= ',' if i < (@facebook_oauth_idp_map.size - 1) %>
<% end -%>
   }
}
