{
  "id": "Google",<%# hardcoded ID %>
  "idpAdapterRef": {
    "id": "Google", 
    "location": "<%= @adm_api_baseURL -%>/idp/adapters/Google"
  },
  "attributeContractFulfillment": {
<% @google_oauth_idp_map.each_with_index do |val, i| -%>
      "<%= val['name'] -%>": {
        "source": {
          "type": "<%= val['type'] -%>"
        },
        "value": "<%= val['value'] -%>"
      }<%= ',' if i < (@google_oauth_idp_map.size - 1) %>
<% end -%>
   }
}
