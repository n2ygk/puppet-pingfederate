{
  "id": "Linkedin",<%# hardcoded ID %>
  "idpAdapterRef": {
    "id": "Linkedin", 
    "location": "<%= @adm_api_baseURL -%>/idp/adapters/Linkedin"
  }, 
  "attributeContractFulfillment": {
<% @linkedin_oauth_idp_map.each_with_index do |val, i| -%>
      "<%= val['name'] -%>": {
        "source": {
          "type": "<%= val['type'] -%>"
        },
        "value": "<%= val['value'] -%>"
      }<%= ',' if i < (@linkedin_oauth_idp_map.size - 1) %>
<% end -%>
  }
}
